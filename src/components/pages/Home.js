import React, { Fragment } from "react";
import InstagramOwner from "../owner/InstagramOwner";
const Home = () => {
  return (
    <Fragment>
      <InstagramOwner />
    </Fragment>
  );
};

export default Home;
